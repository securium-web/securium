# Tests

`static/` contains repository validation and candidate detector tests that run
without Chromium or live network access. Sanitized ChromiumDash responses live
under `fixtures/chromiumdash/`. `integration/` and `security/` reserve explicit
future qualification boundaries. Passing the current tests supports only the
`STATIC` evidence level.
