# Release Automation (Future)

This directory will contain packaging, signing orchestration, provenance, and
publication policy. No package, signing key, release job, or publish path exists
today. Evidence will eventually require all preceding qualification gates,
reviewed artifacts and hashes, protected signing operations, signed updater
metadata, and explicit release authorization.
