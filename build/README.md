# Build Automation (Future)

This directory will contain reproducible Chromium integration and target build
automation. Nothing here builds Chromium today. Evidence will eventually require
an exact source identity, retained toolchain and build configuration, real build
logs, and successful output on approved target infrastructure. Placeholder or
simulated builds must not satisfy `PATCH-APPLY` or `COMPILE`.
