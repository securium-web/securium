# Updater (Future)

This directory will contain updater client/server formats and verification
logic after a separate security design. No updater behavior is implemented or
qualified. Evidence will eventually require independently anchored signature
verification, version and rollback policy tests, metadata/artifact hash binding,
failure-mode tests, and runtime validation.
